#include <stdio.h>
#include "main.h"
/**
 * print_rev - Prints string in reverse
 * @str: String to be reversed
 * Return: void
 */

void _puts(char *str)
{
	int length = 0;

	while (s[length] != '\0')
	{
		length++
	}

	while (length != 0)
	{
		_putchar(str[length]);
		length--;
	}
	_putchar('\n');
}
